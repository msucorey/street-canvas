// Import Actions
import {  } from './PhotoActions';

// Initial State
const initialState = {};

const PhotoReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default PhotoReducer;
